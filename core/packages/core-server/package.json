{
  "name": "@blueearth/core-server",
  "version": "0.1.0",
  "description": "Server-side models, utilities, and database integrations for the BlueEarth Capital portal",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@blueearth/core-common": "^0.1.0",
    "drizzle-orm": "^0.28.5",
    "drizzle-zod": "^0.5.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.0.4",
    "eslint": "^8.48.0",
    "vitest": "^0.34.3"
  }
}